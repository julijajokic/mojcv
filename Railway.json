{
    "build": {
      "api": {
        "builder": "nixpacks",
        "buildCommand": "nix-env -iA nixpkgs.nodejs_18 && npm install --legacy-peer-deps && npm run build && composer install --no-dev --optimize-autoloader"
      }
    },
    "deploy": {
      "api": {
        "startCommand": "php artisan serve --host=0.0.0.0 --port=$PORT",
        "postDeployCommand": "php artisan migrate --force"
      }
    }
  }
  
  
      
  