{
  "build": {
    "api": {
      "builder": "nixpacks",
      "buildCommand": "composer install --no-dev --optimize-autoloader --working-dir=api"
    },
    "/": {
      "builder": "nixpacks",
      "buildCommand": "npm ci --legacy-peer-deps && npm install axios react-router-dom && npm run build"
    }
  },
  "deploy": {
    "api": {
      "startCommand": "php-fpm -D && nginx -g 'daemon off;'",
      "postDeployCommand": "php artisan migrate --force --working-dir=api && php artisan key:generate --working-dir=api"
    },
    "/": {
      "startCommand": "npx serve -s build -l $PORT"
    }
  },
  "config": {
    "platform": {
      "php": "8.3"
    }
  }
}










